node {
    checkout scm

    def customImage = docker.build("my-image:${env.BUILD_ID}", "./Task4")
    post {
        always {
            archiveArtifacts artifacts: customImage, onlyIfSuccessful: true
        }
    }
}
